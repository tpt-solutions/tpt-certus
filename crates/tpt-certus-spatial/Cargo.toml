[package]
name = "tpt-certus-spatial"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
description = "Core spatial engine: ray casting, spatial partitioning (BVH/Octree), broad/narrow-phase collision."

[dependencies]
tpt-certus-math = { workspace = true }
tpt-certus-geometry = { workspace = true }

# Phase 1: telos-generated verified functions will be built via build.rs using
# tpt-telos as a build dependency. For now, no build.rs — scaffold only.

[dev-dependencies]
# Phase 1.3: the mirror<->artifact equivalence cross-checks
# (`tests/mirror_artifact.rs`) consume the realization-layer operation-graph
# mirror from `tpt-certus-proof`, so the executing artifact and the derived `ε`
# envelope are tested against each other in the crate that ships the artifact.
tpt-certus-proof = { workspace = true }

[lints]
workspace = true